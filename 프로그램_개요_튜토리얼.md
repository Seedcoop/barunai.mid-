# 바른AI 챗봇 만들기 (중등용)  
편향/환각 개선 실습 웹앱 개요 및 사용 튜토리얼

## 1. 프로그램 개요

### 목적
이 프로그램은 중등 학습자가 **LLM 기반 챗봇의 편향(bias)과 환각(hallucination)** 문제를 직접 발견하고,  
가이드 문서와 Q&A 카드를 수정해 챗봇 응답의 윤리성을 개선해보는 실습 도구입니다.

### 핵심 학습 포인트
- 같은 질문이라도 가이드 문서 품질에 따라 응답 품질이 달라질 수 있음
- 단정/일반화 표현이 편향 및 환각 위험을 높일 수 있음
- "모르면 모른다고 말하기", "근거/출처 제시" 규칙이 안전한 응답 품질에 중요함

### 화면 구성
1. 상단 탭: `Q&A 실습`, `윤리 가이드 문서`
2. `Q&A 실습` 탭: 왼쪽 `내 Q&A 카드`, 오른쪽 `미리보기 채팅/응답 진단`
3. `윤리 가이드 문서` 탭: 텍스트 입력 + `저장하기` (저장 시 챗봇 반영)

### 진척도 모드(자동)
1. 1단계(탐색형): 취약한 응답 특성이 비교적 자주 나타남
2. 2단계(개선형): 일부 가이드 기준이 반영된 중간 수준 응답
3. 3단계(완성형): 핵심 기준이 반영된 안정적인 응답

## 2. 준비 사항

### 실행 파일
- 메인 페이지: `index.html`
- 스크립트: `app.js`
- 스타일: `styles.css`

### 실행 방법
1. 가장 간단한 방법: `index.html`을 브라우저에서 바로 열기
2. 권장 방법(로컬 서버): 프로젝트 루트의 `launch-local-server.cmd` 실행 후 브라우저 접속

### 0단계 접속 코드
- 앱 시작 전 `실습 시작 코드 입력` 화면이 먼저 나타납니다.
- 코드는 [gate-config.js](C:/Users/sodaj/Desktop/교뵤%20웹앱/중등용/gate-config.js)의 `secretCode` 값을 사용합니다.

### API 사용 관련
- 현재 화면에는 API 설정 입력 UI를 노출하지 않습니다.
- 1단계 취약 챗봇은 로컬 시뮬레이션 모드로 유지됩니다.
- 2~3단계 가이드 적용 모드에서는 Vercel 서버리스 API(`/api/chat`)를 먼저 호출합니다.
- API 오류 또는 환경변수 누락 시 기존 로컬 시뮬레이션으로 자동 전환됩니다.
- API Key는 브라우저 코드에 넣지 않고 Vercel 환경변수 `OPENAI_API_KEY`로만 설정합니다.

## 3. 기본 사용 튜토리얼 (학생 활동)

### Step 1. 취약 챗봇 응답 관찰
1. 초기 상태(진척도 1단계)에서 질문 입력 후 `질문 보내기`를 누릅니다.
3. 응답에서 아래 요소를 찾습니다.
- 근거 없는 단정 표현
- 특정 집단 일반화 표현
- 사실 확인 없이 지어낸 정보

### Step 2. Q&A 카드 수정
1. 왼쪽 `내 Q&A 카드`에서 질문/답변 내용을 수업 주제에 맞게 수정합니다.
2. `Q&A 더 추가하기`로 새 카드 추가
3. 필요 없는 카드는 `삭제` 버튼으로 제거

### Step 3. 가이드 문서 보강
1. 상단 탭에서 `윤리 가이드 문서`를 열고 다음 규칙을 포함하도록 직접 작성합니다.
- 모르면 모른다고 답하기
- 근거 또는 확인 방법 제시하기
- 집단 일반화/차별 표현 피하기
- 학생 눈높이로 설명하기
2. `저장하기`를 눌러 입력 내용을 챗봇에 반영합니다.
3. 저장 후 진척도 단계가 자동으로 조정됩니다.

### Step 4. 개선 결과 비교
1. Step 1에서 사용한 **동일 질문**을 다시 보냅니다.
3. 아래 항목을 비교합니다.
- 편향 위험 점수 변화
- 환각 위험 점수 변화
- 근거성 점수 변화

### Step 5. 공유 및 재현
1. `공유 링크 만들기` 클릭
2. 생성된 링크를 팀원과 공유하여 동일 실습 상태를 재현

## 4. 교사용 운영 가이드 (권장 40~50분)

### 권장 시간 배분
1. 도입(5분): 편향/환각 개념 및 실습 목표 안내
2. 1차 실습(10분): 취약 챗봇에서 문제 응답 수집
3. 2차 실습(15분): 가이드 문서/카드 수정 및 점수 개선
4. 비교/발표(10분): 전후 응답과 지표 변화 공유
5. 정리(5분): 어떤 규칙이 효과적이었는지 회고

### 평가 관점 예시
- 문제 탐지 정확도: 편향/환각 사례를 구체적으로 지적했는가
- 개선 전략 타당성: 가이드 문구가 문제 원인을 직접 다뤘는가
- 결과 검증: 동일 질문 전후 비교를 통해 개선 근거를 제시했는가

## 5. 자주 발생하는 문제와 해결

### API 오류가 날 때
- Vercel 배포 URL 및 `/api/chat` 응답 상태 확인
- Vercel 환경변수 `OPENAI_API_KEY` 등록 여부 확인
- 환경변수 등록 후 재배포 여부 확인
- 네트워크 상태 확인

### 윤리 점수가 낮게 유지될 때
- 가이드 문서에 `근거`, `출처`, `확인 필요`, `일반화 금지` 등 핵심 규칙 문구를 명시
- 단정적 표현(`무조건`, `절대`) 제거

### 응답 개선이 체감되지 않을 때
- 같은 질문으로 전후 비교했는지 확인
- Q&A 카드 답변 자체가 단정적/편향적이지 않은지 점검
- 가이드 문서 분량이 너무 짧지 않은지 확인

## 6. 확장 아이디어
- 반별 템플릿(주제별 기본 Q&A/가이드) 제공
- 실습 결과 저장(전후 응답 로그, 점수 변화 기록)
- 교사용 대시보드(팀별 개선률 비교)
